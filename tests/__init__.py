"""Offline tests for the requirements-engineering assignment.

These tests must NOT require a running Ollama service. HTTP calls into the
local Ollama endpoint are mocked. A separate end-to-end verification with the
real qwen3:8b model is documented in TEST_REPORT.md.
"""
